**Contexto e Missão**
Atua como um Engenheiro de Software Sénior especialista na stack Elixir, Phoenix 1.7 e PostgreSQL. O meu nome é Ítalo e preciso da tua ajuda para finalizar, otimizar ao máximo e preparar para produção o projeto "Nexus Acadêmico" (Event Manager). Este é o projeto prático final para a disciplina de Paradigmas de Linguagens de Programação.

A aplicação já possui a base construída utilizando uma arquitetura de "Contextos Consolidados" (`Core` e `Services`), interfaces reativas com LiveView, e comunicação bidirecional de baixa latência através de Phoenix Channels. O objetivo agora é focar na qualidade do código, performance de baixo nível, resiliência do banco de dados e requisitos da infraestrutura, visando a nota máxima.

**Instruções de Execução:** Divide a tua resposta e os blocos de código nas seguintes 4 fases críticas de otimização:

**Fase 1: Refatoração, Concorrência e Consultas PostgreSQL Avançadas**
1. **Auditoria de Atomicidade (Ecto.Multi):** Analisa a função `Repo.reserve_seat/2` e `Core.cancel_event/1`. Avalia se o isolamento de transação do PostgreSQL atual impede cenários extremos de *race conditions* (overbooking). Se necessário, implementa *Optimistic/Pessimistic Locking* de forma eficiente.
2. **Consultas Complexas:** O ficheiro `Services.ex` tem funções como `get_occupancy_report` e `get_participation_by_course`. Otimiza estas queries utilizando agregações e subconsultas avançadas no Ecto. 
3. **Full-text Search:** Melhora a query de pesquisa no `Repo.search_events`, sugerindo o ficheiro de migração exato para criar os índices adequados (GIN ou GiST) no PostgreSQL.
4. **Otimização de Recursos da VM:** Como prezo por sistemas com baixo consumo de RAM e CPU, garante que a gestão de estado nos processos LiveView (`EventDashboardLive`, `EventChatLive`) não cria *memory leaks* nem mantém *bloatware* em background.

**Fase 2: Arquitetura em Tempo Real e Assincronismo**
1. **Sincronização Websocket:** Revê a lógica do ficheiro `ChatChannel` e a sua integração com o PubSub. Garante que as mensagens enviadas através de *Channels* puros (mobile/clientes externos) são refletidas impecavelmente nos clientes conectados via *LiveView*.
2. **Processamento em Background (Certificados):** Atualmente, `Services.generate_event_certificates/1` pode bloquear o processo se o evento tiver centenas de alunos. Refatora este método para processamento assíncrono (ex: usando `Task.async_stream` ou `Oban`, justificando a escolha), mantendo a tolerância a falhas.

**Fase 3: Infraestrutura DevOps (Integração Nginx)**
De acordo com os requisitos do projeto, preciso de configurar o Nginx.
1. Gera um ficheiro de configuração de *proxy reverso* completo do Nginx (`nginx.conf`) desenhado especificamente para a arquitetura Phoenix.
2. Esta configuração DEVE:
   - Suportar o reencaminhamento adequado do protocolo WebSocket (cabeçalhos `Upgrade` e `Connection`) essenciais para os Channels e LiveView.
   - Servir ficheiros estáticos/uploads diretamente pelo Nginx para poupar ciclos de processamento da aplicação Elixir.
   - Implementar balanceamento e regras básicas de cache.

**Fase 4: Garantia de Qualidade e Documentação de Defesa**
1. Escreve uma suite de testes de integração rápida (`mix test`) focada nos fluxos críticos do `Core.ex` (criação e concorrência na inscrição de eventos).
2. Redige um esqueleto do Relatório Individual final. O texto deve justificar tecnicamente as minhas escolhas de design arquitetural (como os Contextos Consolidados em vez do padrão MVC tradicional), o uso das ferramentas de tempo real e o que foi implementado para garantir a escalabilidade exigida no enunciado do projeto.

**Regras estritas:**
- Adere rigorosamente às convenções do Elixir (formatação do `mix format`, *pattern matching*, *guards*).
- Não inventes novos contextos; todo o código backend deve ser harmonizado dentro das estruturas `Core.ex` e `Services.ex` que já implementei.
- Fornece o código refatorado completo apenas para as funções que alterares, indicando exatamente em que ficheiro elas devem ser substituídas.
